# План: Переработка цветовой схемы и локализация на русский

## Цели
1. Применить новую цветовую схему:
   - `color-background-base` - #141414
   - `color-background-raised` - #333232
   - `color-accent-primary` - #EEE7D2
   - `color-feedback-error` - #AC1414
   - `color-base-black` - #000000
   - `color-base-white` - #FFFFFF

2. Полностью перевести интерфейс на русский язык
3. Не использовать дополнительные библиотеки, только хардкод

---

## Шаги реализации

### 1. Обновить цветовую схему в `src/styles/theme.css`
- Заменить все CSS переменные в `:root` и `.dark` блоках
- Сохранить структуру, но обновить значения цветов согласно схеме
- Убедиться, что все ссылки на цвета в `@theme inline` блоке корректны

### 2. Обновить `index.html`
- Изменить `lang="en"` на `lang="ru"`
- Обновить `<title>` на русское название (например, "Cardify - Генератор изображений")
- Обновить мета-описание при необходимости

### 3. Перевести `src/App-new.tsx` (основной компонент)
Все текстовые строки на русском:
- Заголовок "NanoBanana" → "Cardify" (или оставить, но лучше бренд)
- "Image Generator" → "Генератор изображений"
- "Backend connected" → "Бэкенд подключен"
- "Offline" → "Не в сети"
- "Checking" → "Проверка"
- "Product Title" → "Название товара"
- "Mode" → "Режим"
- "Background" → "Фон"
- "Compose" → "Композиция"
- "Product Image URL" → "URL изображения товара"
- "Upload from Figma selection" → "Загрузить из Figma"
- "Optional fields" → "Дополнительные поля"
- "Price" → "Цена"
- "Features" → "Характеристики"
- "Add a feature..." → "Добавить характеристику..."
- "Style Prompt" → "Описание стиля"
- "Wait for result" → "Ждать результат"
- "Block until image is ready" → "Блокировать до готовности изображения"
- "Use Mock mode" → "Использовать тестовый режим"
- "Test with /api/generate/mock" → "Тестировать через /api/generate/mock"
- "Max wait time (ms)" → "Макс. время ожидания (мс)"
- "Generate Real" → "Сгенерировать (реальный)"
- "Generate Mock" → "Сгенерировать (тестовый)"
- "Ready to generate" → "Готово к генерации"
- "Configure your settings and click Generate" → "Настройте параметры и нажмите Сгенерировать"
- "Processing..." → "Обработка..."
- "Waiting for NanoBanana AI" → "Ожидание ответа от Cardify AI"
- "Task ID" → "ID задачи"
- "Generation complete!" → "Генерация завершена!"
- "Image URL" → "URL изображения"
- "Insert into Figma" → "Вставить в Figma"
- "Download" → "Скачать"
- "Generation failed" → "Генерация не удалась"
- "Try again" → "Попробовать снова"
- "Recent generations" → "Недавние генерации"
- "Backend:" → "Бэкенд:"
- "Settings" → "Настройки"
- "View API Docs" → "Документация API"
- Плейсхолдеры: "White Sneakers" → "Белые кроссовки", "$99" → "99", и т.д.

### 4. Перевести `src/main.tsx` (дублирует App-new.tsx)
Выполнить те же переводы, что и в п.3

### 5. Обновить `README.md`
- Полностью перевести на русский
- Сохранить структуру и информацию
- Заголовок: "Shadcn Figma Plugin" → "Cardify Plugin"
- Обновить описания, инструкции, разделы

### 6. Проверить другие компоненты на английский текст
- `src/components/ui/sonner.tsx` - тема "dark" может остаться, но проверить
- `src/components/ui/calendar.tsx` - дни недели, месяцы (если есть)
- `src/components/ui/select.tsx` - значения по умолчанию
- `src/components/ui/alert-dialog.tsx` - кнопки "Cancel", "Confirm"
- `src/components/ui/dialog.tsx` - заголовки
- `src/components/ui/toast` (если есть кастомные)

### 7. Обновить `package.json` (если есть скрипты с английскими названиями)
- Проверить `name`, `description`
- Обновить на русские или нейтральные названия

### 8. Обновить `tailwind.config.js` при необходимости
- Цвета используют CSS переменные, поэтому основная работа в theme.css
- Убедиться, что нет хардкод цветов в конфиге

---

## Проверки
- [ ] Все видимые тексты на русском
- [ ] Цветовая схема применена корректно
- [ ] Нет английских остатков (кроме кода, имён переменных, имён файлов)
- [ ] Интерфейс читаемый, контрастность сохранена
- [ ] Работает в светлом и тёмном режиме (если поддерживается)

---

## Примечания
- Проект использует shadcn/ui компоненты, которые сами по себе абстрактны
- Основной текст сосредоточен в `App-new.tsx` и `main.tsx`
- Цвета задаются через CSS переменные, что упрощает переключение темы
- Не менять структуру кода, только тексты и цвета
